# To retrieve the code of a key or key combination,
# on a terminal run `cat` and then press the key or
# key combination: the code will be printed on the screen.
#
# If the key combination is intercepted by someone else
# (e.g. the graphic environment) before reaching the terminal,
# you won't see anything and such a key combination won't work.
# In that case you can try to configure the graphic environment
# to not intercept that key combinations.
#
# A : up
# B : down
# C : right
# D : left
#
# 1~ : Home
# 2~ : Insert
# 3~ : Delete
# 4~ : End
# 5~ : PageUp
# 6~ : PageDown
#
# 1;2 : Shift
# 1;3 : Alt
# 1;5 : Ctrl
#
# \t : Tab
# ? : Backspace

# Arrows
"\e[A": history-search-backward
"\e[B": history-search-forward
"\e[C": forward-char
"\e[D": backward-char

# Keys
"\e[1~": beginning-of-line
"\e[3~": delete-char
"\e[4~": end-of-line
"\e[\t": complete
"\e[?": backward-delete-char

# Ctrl + arrow
"\e[1;5C": forward-word
"\e[1;5D": backward-word

# Shift + arrow
"\e[1;2C": kill-line
"\e[1;2D": backward-kill-line
"\e[1;2A": kill-whole-line

# Remove sound
set bell-style none

# Avoid duplicating completion text
set skip-completed-text On
